---
# clang-tidy configuration for VisionCraft (C++20)
#
# C++20 Feature Coverage:
# - Concepts: Enabled via modernize-* checks
# - Ranges: Enabled via modernize-use-ranges-* checks
# - constexpr/consteval: Enabled via modernize-use-constexpr
# - [[nodiscard]]: Enabled via modernize-use-nodiscard
# - Structured bindings: Enabled via modernize-*
#
# The wildcard "*" enables all checks by default, including C++20-aware checks.
# We explicitly disable vendor-specific and opinionated check groups below.
Checks: "*,
        -abseil-*,
        -altera-*,
        -android-*,
        -fuchsia-*,
        -google-*,
        -llvm*,
        -modernize-use-trailing-return-type,
        -zircon-*,
        -readability-else-after-return,
        -readability-static-accessed-through-instance,
        -readability-avoid-const-params-in-decls,
        -cppcoreguidelines-non-private-member-variables-in-classes,
        -misc-non-private-member-variables-in-classes,
"
WarningsAsErrors: ''
HeaderFilterRegex: ''
FormatStyle:     none

CheckOptions:
  - key: readability-identifier-length.IgnoredVariableNames
    value: 'x|y|z'
  - key: readability-identifier-length.IgnoredParameterNames
    value: 'x|y|z'
